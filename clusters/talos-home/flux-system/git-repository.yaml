apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/jobdouma/k8s-home-server.git
  ref:
    branch: main
  secretRef:
    name: flux-system
