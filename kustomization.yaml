apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Flux bootstrap components (generated by flux bootstrap)
  - flux-system

  # Flux reconciliation entrypoints
  - infrastructure-flux.yaml
  - apps-flux.yaml
