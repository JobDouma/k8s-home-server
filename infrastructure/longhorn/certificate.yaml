apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: longhorn-tls
  namespace: networking
spec:
  secretName: longhorn-tls
  duration: 8760h
  renewBefore: 720h
  dnsNames:
    - longhorn.lan
  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
    group: cert-manager.io