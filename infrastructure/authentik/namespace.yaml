# authentik/namespace.yaml — fix labels → annotations
apiVersion: v1
kind: Namespace
metadata:
  name: authentik
  annotations:
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/warn: restricted   # warns but doesn't block