apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media
  namespace: media
  labels:
    backup.velero.io/exclude-from-backup: "true"
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Ti
  volumeName: pv-media
