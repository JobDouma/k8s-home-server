apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-dashboard
  namespace: networking
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`traefik.lan`)
      kind: Rule
      services:
        - name: traefik
          port: 9000
