apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: traefik-tls
  namespace: networking
spec:
  secretName: traefik-tls
  duration: 8760h
  renewBefore: 720h
  dnsNames:
    - traefik.lan
  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
    group: cert-manager.io