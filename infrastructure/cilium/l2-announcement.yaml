apiVersion: cilium.io/v2alpha1
kind: CiliumL2AnnouncementPolicy
metadata:
  name: lan
spec:
  nodeSelector:
    matchLabels: {}
  serviceSelector:
    matchLabels: {}
  externalIPs: true
  loadBalancerIPs: true   # <-- was missing; announces LB-IPAM assigned IPs via ARP