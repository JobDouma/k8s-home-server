apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argus-tls
  namespace: argus
spec:
  secretName: argus-tls
  duration: 8760h
  renewBefore: 720h
  dnsNames:
    - argus.lan
  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
    group: cert-manager.io