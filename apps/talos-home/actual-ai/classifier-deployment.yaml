apiVersion: apps/v1
kind: Deployment
metadata:
  name: classifier-api
  namespace: actual-ai
spec:
  replicas: 1
  selector:
    matchLabels:
      app: classifier-api
  template:
    metadata:
      labels:
        app: classifier-api
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: api
          image: python:3.11.9-slim@sha256:8fb099199b9f2d70342674bd9dbccd3ed03a258f26bbd1d556822c6dfc60c317
          command:
            - /bin/sh
            - -c
            - |
              pip install --quiet --target /tmp/pypackages fastapi uvicorn pyyaml requests && \
              cp /script/app.py /tmp/app.py && \
              PYTHONPATH=/tmp/pypackages python -m uvicorn app:app --app-dir /tmp --host 0.0.0.0 --port 8080
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: config
              mountPath: /config
            - name: script
              mountPath: /script
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: ["ALL"]
      volumes:
        - name: config
          configMap:
            name: classifier-config
        - name: script
          configMap:
            name: classifier-script