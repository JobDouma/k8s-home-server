apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: joplin-tls
  namespace: media
spec:
  secretName: joplin-tls
  duration: 8760h
  renewBefore: 720h
  dnsNames:
    - joplin.lan
  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
    group: cert-manager.io