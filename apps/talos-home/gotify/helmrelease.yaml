apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: gotify
  namespace: gotify
spec:
  interval: 15m
  timeout: 10m
  maxHistory: 2
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  chart:
    spec:
      chart: app-template
      version: "5.1.0"
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  values:
    controllers:
      gotify:
        containers:
          app:
            image:
              repository: gotify/server
              tag: "3.0.0@sha256:d75e89e0e28389c00c2556afe01282a37ee9756b0285799aa25214243aebd5e5"
            env:
              TZ: Europe/Amsterdam
              GOTIFY_DEFAULTUSER_NAME:
                valueFrom:
                  secretKeyRef:
                    name: gotify-secret
                    key: admin-user
              GOTIFY_DEFAULTUSER_PASS:
                valueFrom:
                  secretKeyRef:
                    name: gotify-secret
                    key: admin-password
            ports:
            - name: http
              containerPort: 8080   # changed from 80
              protocol: TCP
            # Removed NET_BIND_SERVICE capability
            probes:
              readiness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /health
                    port: 8080      # updated
                  initialDelaySeconds: 5
                  periodSeconds: 5
                  failureThreshold: 6
              liveness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /health
                    port: 8080      # updated
                  initialDelaySeconds: 10
                  periodSeconds: 10
                  failureThreshold: 3
            resources:
              requests:
                cpu: 50m
                memory: 64Mi
              limits:
                memory: 256Mi
        pod:
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            fsGroup: 1000
            runAsNonRoot: true
    service:
      app:
        controller: gotify
        type: ClusterIP
        ports:
          http:
            port: 80              # external service port remains 80
            targetPort: http      # points to container port 8080
    persistence:
      data:
        type: persistentVolumeClaim
        storageClass: local-path
        accessMode: ReadWriteOnce
        size: 1Gi
        globalMounts:
        - path: /app/data