apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gotify-tls
  namespace: gotify
spec:
  secretName: gotify-tls
  duration: 8760h
  renewBefore: 720h
  dnsNames:
    - gotify.lan
  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
    group: cert-manager.io