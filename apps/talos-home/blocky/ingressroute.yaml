apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: blocky
  namespace: dns
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`blocky.lan`)
      kind: Rule
      services:
        - name: blocky
          port: 4000
