apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: blocky
  namespace: dns
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: blocky
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
