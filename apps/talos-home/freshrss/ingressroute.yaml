apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: freshrss
  namespace: freshrss
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`freshrss.lan`)
      kind: Rule
      services:
        - name: freshrss
          port: 80
