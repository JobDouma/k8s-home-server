apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: freshrss-tls
  namespace: freshrss
spec:
  secretName: freshrss-tls
  duration: 8760h
  renewBefore: 720h
  dnsNames:
    - freshrss.lan
  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
    group: cert-manager.io