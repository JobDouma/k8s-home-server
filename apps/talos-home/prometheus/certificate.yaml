apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: prometheus-tls
  namespace: media
spec:
  secretName: prometheus-tls
  duration: 8760h
  renewBefore: 720h
  dnsNames:
    - prometheus.lan
  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
    group: cert-manager.io