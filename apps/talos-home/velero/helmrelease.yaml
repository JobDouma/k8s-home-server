apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: velero
  namespace: velero
spec:
  interval: 30m
  timeout: 20m
  chart:
    spec:
      chart: velero
      version: "11.4.0"
      sourceRef:
        kind: HelmRepository
        name: vmware-tanzu
        namespace: flux-system
  values:
    upgradeCRDs: false

    initContainers:
      - name: velero-plugin-for-aws
        image: velero/velero-plugin-for-aws:v1.13.2
        imagePullPolicy: IfNotPresent
        volumeMounts:
          - mountPath: /target
            name: plugins

    configuration:
      defaultVolumesToFsBackup: false
      backupStorageLocation:
        - name: default
          provider: aws
          bucket: velero
          default: true
          accessMode: ReadWrite
          config:
            region: minio
            s3ForcePathStyle: "true"
            s3Url: http://minio.velero.svc.cluster.local:9000
      maintenanceJobConfig:
        jobTTL: 1h0m0s
        keepLatestMaintenanceJobs: 1   # ← only keep 1 completed job per repo
      volumeSnapshotLocation: []

    credentials:
      useSecret: true
      existingSecret: velero-minio-credentials
      existingSecretKey: cloud

    # No node-agent needed — no filesystem backup
    deployNodeAgent: false

    metrics:
      enabled: true
      serviceMonitor:
        enabled: false

    schedules:
      daily:
        disabled: false
        schedule: "0 3 * * *"
        template:
          ttl: 720h
          snapshotVolumes: false
          defaultVolumesToFsBackup: false
          # Skip PVC/PV data — Longhorn handles that
          excludedResources:
            - persistentvolumes
            - persistentvolumeclaims
          excludedNamespaces:
            - metallb-system
            - networking
            - velero
            - falco