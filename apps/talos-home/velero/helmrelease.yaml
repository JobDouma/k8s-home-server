apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: velero
  namespace: velero
spec:
  interval: 30m
  timeout: 20m

  chart:
    spec:
      chart: velero
      version: "11.4.0"
      sourceRef:
        kind: HelmRepository
        name: vmware-tanzu
        namespace: flux-system

  values:
    upgradeCRDs: false

    configuration:
      backupStorageLocation:
        - name: default
          provider: velero.io/fs
          bucket: velero
          default: true
          accessMode: ReadWrite
          config:
            path: /backups

      volumeSnapshotLocation: []

    deployNodeAgent: true

    persistence:
      enabled: true
      existingClaim: velero-backup-storage

    metrics:
      enabled: true
      serviceMonitor:
        enabled: false  # we manage our own above
    schedules:
      daily:
        disabled: false
        schedule: "0 3 * * *"
        template:
          ttl: 720h
          snapshotVolumes: false