apiVersion: batch/v1
kind: Job
metadata:
  name: uptime-kuma-provisioner
  namespace: uptime-kuma
  annotations:
    # To re-run: delete this Job and let Flux recreate it.
    # To force re-provision: also delete /app/data/.monitors-provisioned from the PVC.
spec:
  backoffLimit: 10
  template:
    spec:
      restartPolicy: OnFailure

      initContainers:
        - name: wait-for-kuma
          image: busybox:1.37.0@sha256:1487d0af5f52b4ba31c7e465126ee2123fe3f2305d638e7827681e7cf6c83d5e
          command:
            - /bin/sh
            - -c
            - |
              echo "Waiting for Uptime Kuma..."
              until wget -qO- http://uptime-kuma.uptime-kuma.svc.cluster.local:3001 >/dev/null 2>&1; do
                echo "Not ready yet, retrying in 5s..."
                sleep 5
              done
              echo "Uptime Kuma is ready."

      containers:
        - name: provisioner
          image: python:3.14-slim@sha256:fb83750094b46fd6b8adaa80f66e2302ecbe45d513f6cece637a841e1025b4ca
          command:
            - python3
            - /scripts/provision.py
          env:
            - name: UPTIME_KUMA_ADMIN_USER
              valueFrom:
                secretKeyRef:
                  name: uptime-kuma-credentials
                  key: admin-user
            - name: UPTIME_KUMA_ADMIN_PASS
              valueFrom:
                secretKeyRef:
                  name: uptime-kuma-credentials
                  key: admin-pass
            - name: UPTIME_KUMA_SERVER
              value: "http://uptime-kuma.uptime-kuma.svc.cluster.local:3001"
          volumeMounts:
            - name: scripts
              mountPath: /scripts/provision.py
              subPath: provision.py
              readOnly: true
            - name: data
              mountPath: /app/data

      volumes:
        - name: scripts
          configMap:
            name: uptime-kuma-monitors
            defaultMode: 0755
        - name: data
          persistentVolumeClaim:
            claimName: uptime-kuma