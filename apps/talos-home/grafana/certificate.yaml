apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grafana-tls
  namespace: media
spec:
  secretName: grafana-tls
  duration: 8760h
  renewBefore: 720h
  dnsNames:
    - grafana.lan
  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
    group: cert-manager.io