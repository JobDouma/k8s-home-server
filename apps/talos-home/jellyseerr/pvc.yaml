apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jellyseerr-config
  namespace: media
spec:
  accessModes:
    - ReadWriteMany

  storageClassName: truenas-nfs-v2

  resources:
    requests:
      storage: 5Gi
