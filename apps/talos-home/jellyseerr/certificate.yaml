apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: jellyseerr-tls
  namespace: media
spec:
  secretName: jellyseerr-tls
  duration: 8760h
  renewBefore: 720h
  dnsNames:
    - jellyseerr.lan
  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
    group: cert-manager.io